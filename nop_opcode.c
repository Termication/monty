#include "monty.h"
/**
 * nop_opcode - does nothing
 * @stack: pointer to the top of the stack
 * @encounter: line number where the operation is encountered
 *
 * Description: This function does nothing and is used as a placeholder
 * for operations that have no effect on the stack.
 */
void nop_opcode(stack_t **stack, unsigned int encounter)
{
	(void)stack;
	(void)encounter;
}
